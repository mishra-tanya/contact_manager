<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Navbar</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
        .card {
            max-width: 500px;
            margin: auto;
        }
   
  </style>
</head>
<body>
  <div class="detail m-3 row justify-content-center">
        <div class="card shadow-lg m-3 p-3"id="addcontact">
            <h1 class="text-center mt-3 mb-3" style="font-family: Lucida Console, Courier, monospace;">Update Contact
                Details </h1>
            {{#if message}}
            <div class="alert {{ alertClass }}" role="alert">
                {{ message }}
            </div>
            {{/if}}
            <div class="card-body " id="addcontact">
                <form action="/dashboard/{{userId}}/edit/{{contact.id}}" method="POST">

                    <div class="mb-3 row">
                        <label for="name" class="col-sm-4 col-form-label">Contact Name:</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" required id="name"value=" {{contact.name}}" name="name">
                        </div>
                    </div>
                    <div class="mb-3 row">
                        <label for="contact" class="col-sm-4 col-form-label">Contact No :</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" required id="contact"value=" {{contact.contact}}" name="contact">
                        </div>
                    </div>
                    <div class="mb-3 row">
                        <label for="contact" class="col-sm-4 col-form-label"> Address :</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" required id="contact"value=" {{contact.address}}" name="address">
                        </div>
                    </div>
                    <div class="mb-3 row">
                        <label for="email" class="col-sm-4 col-form-label"> Email :</label>
                        <div class="col-sm-8">
                            <input type="email" class="form-control" required id="email"value=" {{contact.email}}" name="email">
                        </div>
                    </div>

                    <div class="my-3">
                        <input type="submit" class="form-control btn btn-dark" value="Add">
                    </div>

                </form>
            </div>
        </div>
    </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
